const { AnomalyDetectorClient } = require('@azure/ai-anomaly-detector');
const { AzureKeyCredential } = require('@azure/core-auth');
const config = require('./config')


module.exports.annomalydetection=async function(){

    try{

    }

    catch(err){

        
    }
}